<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Movie Database</title>
    <style>
  

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Noto Sans", sans-serif;
      }

      html,
      body {
        height: 100%;
      }

      body {
        display: grid;
        place-items: center;
        background: black;
      }

      .container {
        width: 400px;
        background: yellow;
        text-align: center;
        padding: 20px;
        padding-top: 50px;
        padding-bottom: 50px;
        border-radius: 6px;
        border-radius: 6px;
        background: blue;
        box-shadow: -6px 6px 12px #b4b4b4, 6px -6px 12px #cccccc;
      }

      .text {
        font-size: 24px;
        margin-bottom: 24px;
        font-weight: 700;
        color: black;
      }

      .container .field {
        height: 40px;
        width: 100%;
        display: flex;
        color: black;
        position: relative;
        margin-bottom: 10px;
      }

      .field input {
        height: 100%;
        width: 100%;
        padding-left: 38px;
        font-size: 15px;
        border: none;
        color: black;
        outline: none;
        border-radius: 6px;
        background: yellow;
        box-shadow: inset -6px 6px 12px #b4b4b4, inset 6px -6px 12px #cccccc;
      }

      .field i {
        position: absolute;
        width: 45px;
        line-height: 40px;
      }

      .field label {
        position: absolute;
        top: 50%;
        transform: translateY(-50%);
        left: 40px;
        pointer-events: none;
      }

      .field input:focus ~ label {
        opacity: 0;
      }

      .login_btn {
        margin-top: 10px;
        width: 100%;
        height: 40px;
        border: none;
        outline: none;
        cursor: pointer;
        font-size: 13px;
        color: #f3ebeb;
        border-radius: 6px;
        background: blue;
        box-shadow: -6px 6px 12px #b4b4b4, inset 6px -6px 12px #cccccc;
      }

      .login_btn:active {
        background: blue;
        box-shadow: inset -6px 6px 12px #b4b4b4, inset 6px -6px 12px #cccccc;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="text">DEEPU ADMIN</div>
      <form method="POST">
        <div class="field"><input type="text" name="moviename" placeholder="movie-name" /> </div>
        <div class="field">
          <input type="text" name="moviedownloadurl" placeholder="moviedownloadurl"/> 
        </div>
        <div class="field"><input type="text" name="moviedesc" placeholder="moviedesc" /> </div>

        <div class="field">
          <input type="text" name="imageurl" placeholder="imgurl"/> 
        </div>
        <button type="submit"  class="login_btn">Submit</button>
      </form>
    </div>
  </body>
</html>
